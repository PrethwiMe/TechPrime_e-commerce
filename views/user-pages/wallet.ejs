<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Wallet | TechCart</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" />
  <style>
    :root {
      --primary-gradient: linear-gradient(135deg, #020202 0%, #cab3b3 100%);
      --success-gradient: linear-gradient(135deg, #070707 0%, #100e0e 100%);
      --dark-bg: #1a1a1a;
      --white-text: #ffffff;
      --light-bg: #f8f9fa;
    }
    
    * {
      box-sizing: border-box;
    }
    
    body {
      background-color: var(--light-bg);
      margin: 0;
      padding: 0;
      overflow-x: hidden;
    }
    
    /* Loading Spinner Styles */
    .loading-overlay {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(255, 255, 255, 0.95);
      display: flex;
      justify-content: center;
      align-items: center;
      z-index: 9999;
      transition: opacity 0.3s ease;
    }
    
    .loading-overlay.hidden {
      opacity: 0;
      pointer-events: none;
    }
    
    .spinner-container {
      text-align: center;
    }
    
    .wallet-spinner {
      width: 80px;
      height: 80px;
      border: 6px solid #f3f3f3;
      border-top: 6px solid #020202;
      border-radius: 50%;
      animation: spin 1s linear infinite;
      margin: 0 auto 20px;
    }
    
    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }
    
    .loading-text {
      font-size: 1.2rem;
      color: #333;
      font-weight: 600;
      margin-top: 10px;
    }
    
    .loading-subtext {
      font-size: 0.9rem;
      color: #666;
      margin-top: 5px;
    }
    
    /* Main Content - Initially Hidden */
    .main-content {
      opacity: 0;
      transition: opacity 0.5s ease;
      min-height: 100vh;
    }
    
    .main-content.loaded {
      opacity: 1;
    }
    
    /* Content Wrapper - Fixed for sidebar layout */
    .content-wrapper {
      margin-left: 250px;
      padding: 20px;
      width: calc(100% - 250px);
      min-height: 100vh;
    }
    
    .balance-card {
      background: var(--primary-gradient);
      color: var(--white-text);
      border-radius: 20px;
      box-shadow: 0 15px 35px rgba(0, 0, 0, 0.15);
      transition: transform 0.3s ease;
    }
    
    .balance-card:hover {
      transform: translateY(-5px);
    }
    
    .balance-amount {
      font-size: 3rem;
      font-weight: 700;
      color: var(--white-text);
      text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
    }
    
    .transaction-item {
      background: var(--white-text);
      border: none;
      border-radius: 15px;
      box-shadow: 0 5px 20px rgba(0, 0, 0, 0.08);
      transition: all 0.3s ease;
      margin-bottom: 15px;
    }
    
    .transaction-item:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.12);
    }
    
    .credit-badge {
      background: var(--success-gradient);
      color: var(--white-text);
      border-radius: 20px;
      padding: 0.5rem 1rem;
      font-weight: 600;
    }
    
    .debit-badge {
      background: linear-gradient(135deg, #dc3545 0%, #c82333 100%);
      color: var(--white-text);
      border-radius: 20px;
      padding: 0.5rem 1rem;
      font-weight: 600;
    }
    
    .header-section {
      background: var(--primary-gradient);
      color: var(--white-text);
      border-radius: 15px 15px 0 0;
      padding: 1.5rem;
    }
    
    .no-transactions {
      color: #6c757d;
      text-align: center;
      padding: 3rem;
    }
    
    /* Pagination Styles */
    .pagination {
      margin: 1rem 0;
    }
    
    .pagination .page-link {
      color: #020202;
      border-radius: 8px;
      margin: 0 3px;
    }
    
    .pagination .page-item.active .page-link {
      background: var(--primary-gradient);
      border-color: #020202;
    }
    
    /* Responsive Design */
    @media (max-width: 768px) {
      .balance-amount {
        font-size: 2.5rem;
      }
      
      .content-wrapper {
        margin-left: 0 !important;
        width: 100% !important;
        padding: 10px;
      }
      
      .transaction-item {
        flex-direction: column;
        text-align: center;
      }
      
      .transaction-item .text-end {
        text-align: center !important;
        margin-top: 1rem;
        margin-left: 0 !important;
      }
    }
    
    @media (max-width: 576px) {
      .balance-amount {
        font-size: 2rem;
      }
      
      .content-wrapper {
        padding: 10px 5px;
      }
    }
  </style>
</head>

<body>
  <!-- Loading Spinner Overlay -->
  <div class="loading-overlay" id="loadingOverlay">
    <div class="spinner-container">
      <div class="wallet-spinner"></div>
      <div class="loading-text">
        <i class="fas fa-wallet me-2"></i>Loading Wallet
      </div>
      <div class="loading-subtext">Please wait while we fetch your data...</div>
    </div>
  </div>

  <!-- Main Content -->
  <div class="main-content" id="mainContent">
    <!-- Header and Sidebar (These are included from partials) -->
    <%- include('partials/header') %>
    <%- include('partials/accountSidebar') %>

    <!-- Wallet Content Wrapper -->
    <div class="content-wrapper">
      <div class="container-fluid">
        <!-- Welcome Section -->
        <div class="row mb-4">
          <div class="col-12">
            <div class="d-flex align-items-center">
              <i class="fas fa-wallet fa-2x text-primary me-3"></i>
              <div>
                <h2 class="mb-0 fw-bold text-dark">Welcome, <%= user?.firstName || "Customer" %>!</h2>
                <p class="text-muted mb-0">Manage your wallet balance and view your transaction history.</p>
              </div>
            </div>
          </div>
        </div>

        <!-- Wallet Balance Card -->
        <div class="row mb-5 justify-content-center">
          <div class="col-12 col-md-8 col-lg-6">
            <div class="card balance-card p-4 text-center">
              <i class="fas fa-coins fa-3x mb-3 opacity-90 text-warning"></i>
              <h1 class="balance-amount mb-2">
                ₹<%= (wallet?.walletAmount ? wallet.walletAmount.toLocaleString('en-IN') : '0') %>
              </h1>
              <p class="lead mb-2">Available Wallet Balance</p>
              <small class="opacity-90">
                <i class="fas fa-clock me-1"></i>
                Last updated: <%= wallet?.updatedDate ? new Date(wallet.updatedDate).toLocaleString('en-IN') : 'N/A' %>
              </small>
            </div>
          </div>
        </div>

        <!-- Wallet Transaction History -->
        <div class="row">
          <div class="col-12">
            <div class="card shadow-lg border-0 rounded-4 overflow-hidden">
              <div class="header-section">
                <div class="d-flex justify-content-between align-items-center flex-wrap">
                  <h3 class="mb-0 fw-bold">
                    <i class="fas fa-history me-2"></i>Transaction History
                  </h3>
                  <span class="badge bg-light text-dark fs-6 px-3 py-2 rounded-pill">
                    <%= refundHistory.length %> Transactions
                  </span>
                </div>
              </div>
              <div class="card-body p-0">
                <div id="transactionsContainer">
                  <% if (refundHistory && refundHistory.length > 0) { %>
                    <div class="p-4">
                      <div class="row g-3">
                        <% refundHistory.forEach(function(txn) { %>
                          <% 
                            var isCredit = txn.refundAmount !== undefined;
                            var type = isCredit ? 'credit' : 'debit';
                            var amount = isCredit ? txn.refundAmount : txn.amount;
                            var description = isCredit ? txn.orderId : 'Order Payment';
                            var status = txn.status || null;
                            var iconClass = type === 'credit' ? 'bg-success bg-opacity-10 text-success' : 'bg-danger bg-opacity-10 text-danger';
                            var icon = type === 'credit' ? 'fa-arrow-up' : 'fa-arrow-down';
                            var amountSign = type === 'credit' ? '+' : '-';
                            var amountColor = type === 'credit' ? 'text-success' : 'text-danger';
                            var badgeClass = type === 'credit' ? 'credit-badge' : 'debit-badge';
                            var badgeText = type === 'credit' ? '<i class="fas fa-check-circle me-1"></i>Credited' : '<i class="fas fa-minus-circle me-1"></i>Debited';
                            var statusBadge = status ? `<small class="badge bg-success bg-opacity-25 text-success mt-1">${status}</small>` : '';
                            var formattedDate = new Date(txn.date).toLocaleDateString('en-IN');
                            var formattedTime = new Date(txn.date).toLocaleTimeString('en-IN', { hour: '2-digit', minute: '2-digit' });
                          %>
                          <div class="col-12">
                            <div class="transaction-item p-4 d-flex align-items-center justify-content-between">
                              <div class="d-flex align-items-center flex-grow-1">
                                <div class="<%= iconClass %> rounded-circle p-3 me-4">
                                  <i class="fas <%= icon %> fa-lg"></i>
                                </div>
                                <div class="flex-grow-1">
                                  <h6 class="mb-1 fw-semibold text-dark"><%= description %></h6>
                                  <small class="text-muted d-block">
                                    <i class="fas fa-calendar-day me-1"></i>
                                    <%= formattedDate %> • <%= formattedTime %>
                                  </small>
                                  <%- statusBadge %>
                                </div>
                              </div>
                              <div class="text-end ms-3">
                                <h4 class="mb-1 <%= amountColor %> fw-bold"><%= amountSign %>₹<%= amount.toLocaleString('en-IN') %></h4>
                                <span class="<%= badgeClass %>"><%- badgeText %></span>
                              </div>
                            </div>
                          </div>
                        <% }); %>
                      </div>
                    </div>
                  <% } else { %>
                    <div class="no-transactions">
                      <i class="fas fa-inbox fa-4x mb-3"></i>
                      <h4 class="mb-2">No transactions yet</h4>
                      <p class="mb-0">Your wallet history will appear here once you receive refunds or credits.</p>
                    </div>
                  <% } %>
                </div>
              </div>
              <!-- Server-Side Pagination Controls -->
              <% if (totalPages > 1) { %>
                <nav aria-label="Transaction pagination" class="px-3">
                  <ul class="pagination justify-content-center mb-3">
                    <% if (currentPage > 1) { %>
                      <li class="page-item">
                        <a class="page-link" href="?page=<%= currentPage - 1 %>" aria-label="Previous">
                          <span aria-hidden="true">&laquo; Previous</span>
                        </a>
                      </li>
                    <% } %>
                    <% for (let i = 1; i <= totalPages; i++) { %>
                      <li class="page-item <%= i === currentPage ? 'active' : '' %>">
                        <a class="page-link" href="?page=<%= i %>"><%= i %></a>
                      </li>
                    <% } %>
                    <% if (currentPage < totalPages) { %>
                      <li class="page-item">
                        <a class="page-link" href="?page=<%= currentPage + 1 %>" aria-label="Next">
                          <span aria-hidden="true">Next &raquo;</span>
                        </a>
                      </li>
                    <% } %>
                  </ul>
                </nav>
              <% } %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  
  <script>
    // Initialize on load - Hide loading spinner
    window.addEventListener('load', function() {
      const loadingOverlay = document.getElementById('loadingOverlay');
      const mainContent = document.getElementById('mainContent');
      
      // Hide spinner after brief delay for smooth transition
      setTimeout(function() {
        loadingOverlay.classList.add('hidden');
        mainContent.classList.add('loaded');
        setTimeout(function() {
          loadingOverlay.style.display = 'none';
        }, 300);
      }, 500);
    });
    
    // Fallback: Hide spinner if it takes too long
    setTimeout(function() {
      const loadingOverlay = document.getElementById('loadingOverlay');
      if (!loadingOverlay.classList.contains('hidden')) {
        loadingOverlay.classList.add('hidden');
        document.getElementById('mainContent').classList.add('loaded');
      }
    }, 2000);
  </script>
</body>

</html>