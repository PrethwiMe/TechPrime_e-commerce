<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Set New Password - TechPrime</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

  <style>
    body {
      background-color: #f8f9fa;
      font-family: 'Segoe UI', sans-serif;
    }

    .reset-password-container {
      max-width: 480px;
      margin: 80px auto;
      background: white;
      padding: 30px 35px;
      border-radius: 12px;
      box-shadow: 0 0 15px rgba(0,0,0,0.1);
    }

    .page-title {
      font-weight: 600;
      color: #1a73e8;
      margin-bottom: 1.5rem;
      text-align: center;
    }

    label {
      font-weight: 600;
    }

    .form-control {
      border-radius: 8px;
      font-size: 1rem;
      padding: 10px 15px;
      transition: border-color 0.3s ease;
    }

    .form-control:focus {
      border-color: #1a73e8;
      box-shadow: 0 0 8px rgba(26, 115, 232, 0.3);
    }

    .btn-techprime {
      background-color: #1a73e8;
      border: none;
      color: white;
      font-weight: 600;
      padding: 12px;
      width: 100%;
      border-radius: 8px;
      font-size: 1.1rem;
      transition: background-color 0.3s ease;
    }

    .btn-techprime:hover {
      background-color: #155fc0;
    }

    .text-center {
      text-align: center;
    }

    .text-danger {
      margin-top: 10px;
    }
  </style>
</head>
<body>

  <%- include('partials/header') %>

  <div class="reset-password-container" role="main" aria-label="Set New Password">
    <h2 class="page-title">Set Your New Password</h2>

    <form action="/verify-otp/reset-password" method="POST" novalidate>
      <input type="hidden" name="email" value="<%= email %>" />

      <div class="mb-3">
        <label for="password" class="form-label">New Password</label>
        <input
          type="password"
          id="password"
          name="password"
          class="form-control"
          placeholder="Enter new password"
          minlength="6"
          
          autocomplete="new-password"
          aria-describedby="passwordHelp"
        />
        <div id="passwordHelp" class="form-text">Must be at least 6 characters.</div>
      </div>

      <div class="mb-4">
        <label for="confirmPassword" class="form-label">Confirm New Password</label>
        <input
          type="password"
          id="confirmPassword"
          name="confirmPassword"
          class="form-control"
          placeholder="Confirm new password"
          minlength="6"
          
          autocomplete="new-password"
        />
      </div>

      <button type="submit" class="btn btn-techprime">Save Password</button>

      <% if (err) { %>
        <p class="text-danger text-center"><%= err %></p>
      <% } %>

      <% if (success) { %>
        <p class="text-success text-center"><%= success %></p>
      <% } %>
    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
